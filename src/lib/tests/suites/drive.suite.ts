import type { TestDef } from "../../../types/test.types";
import { registerSuite } from "../registry";

// Placeholder — fill in once QA spec is accessible and endpoint shapes are confirmed
const tests: TestDef[] = [];

registerSuite(tests);

export default tests;
